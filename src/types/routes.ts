/**
 * provides collection of routes
 */
enum ROUTE {
  HOME = "/",
  LOGIN = "/login",
  PROFILE = "/profile",
  ACCOLADES = "/accolades",
  CREATE = "/create",
  ABOUT = "/about",
  PLAYLIST = "/playlist",
  PROJECTS = "/projects",
}

export default ROUTE;
